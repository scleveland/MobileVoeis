<div class="pageTitle">
  <h1>Edit <%= @projects.name %></h1>
</div>

<div class="toolbar">
  <div class="leftItem backButton">
    <a href="<%= url_for :action => :show, :id => @projects.object %>">Cancel</a>
  </div>
  <div class="rightItem regularButton">
    <a class="button" href="<%= url_for :action => :delete, :id => @projects.object %>">Delete</a>
  </div>
</div>

<div class="content">
  <form method="POST" action="<%= url_for :action => :update %>">
    <input type="hidden" name="id" value="<%= @projects.object %>"/>
    <ul>
      
          <li>
            <label for="projects[name]" class="fieldLabel">Name</label>
            <input type="text" name="projects[name]" value="<%= @projects.name %>" <%= placeholder( "Name" ) %> />
          </li>
      
    </ul>
    <input type="submit" class="standardButton" value="Update"/>
  </form>
</div>